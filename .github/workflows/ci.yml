name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: C tests
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            z3 \
            libz3-dev

          python3 -m pip install --upgrade pip
          python3 -m pip install z3-solver

      - name: Show tool versions
        run: |
          python3 --version
          python3 -c "import z3; print('Z3 Python:', z3.get_version_string())"
          z3 --version
          cc --version
          make --version

      - name: Build and run C test suite
        run: make test


  formal:
    name: Formal verification
    runs-on: ubuntu-latest
    needs: test

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Z3
        run: |
          sudo apt-get update
          sudo apt-get install -y z3

          python3 -m pip install --upgrade pip
          python3 -m pip install z3-solver

      - name: Verify Z3
        run: |
          z3 --version
          python3 -c "import z3; print('Z3 Python:', z3.get_version_string())"

      - name: Run formal verification
        run: make formal

      - name: Verify generated SMT files
        shell: bash
        run: |
          set -euo pipefail

          shopt -s nullglob
          files=(formal/*.smt2)

          if [ "${#files[@]}" -eq 0 ]; then
            echo "ERROR: no SMT2 proof files were generated"
            exit 1
          fi

          echo "Generated SMT files:"
          printf '  %s\n' "${files[@]}"


  collision-finder:
    name: Collision Finder Research Tests
    runs-on: ubuntu-latest
    needs: formal
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Z3
        run: |
          sudo apt-get update
          sudo apt-get install -y z3

          python3 -m pip install --upgrade pip
          python3 -m pip install z3-solver

      - name: Verify Z3 installation
        run: |
          z3 --version
          python3 -c "import z3; print('Z3 Python:', z3.get_version_string())"

      - name: Verify collision finder exists
        run: |
          test -f benchmark/collision_finder.py

      - name: Python syntax check
        run: |
          python3 -m py_compile benchmark/collision_finder.py

      - name: Run collision finder self-test
        run: |
          python3 benchmark/collision_finder.py \
            --self-test

      - name: Run 39-round known construction
        run: |
          python3 benchmark/collision_finder.py \
            --rounds 39 \
            --timeout 60

      - name: Run 16-round differential-window experiment
        run: |
          python3 benchmark/collision_finder.py \
            --rounds 39 \
            --zero-start 12 \
            --zero-rounds 16 \
            --timeout 60 \
            --dump-smt collision_window.smt2

      - name: Verify SMT export
        run: |
          test -s collision_window.smt2

      - name: Upload differential SMT model
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: collision-differential-smt
          path: collision_window.smt2
          if-no-files-found: warn


  benchmark-equivalence:
    name: Symbolic equivalence benchmark
    runs-on: ubuntu-latest
    needs: formal
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Z3
        run: |
          sudo apt-get update
          sudo apt-get install -y z3

          python3 -m pip install --upgrade pip
          python3 -m pip install z3-solver

      - name: Verify Z3
        run: |
          z3 --version
          python3 -c "import z3; print('Z3 Python:', z3.get_version_string())"

      - name: Run Python syntax check
        run: |
          python3 -m py_compile \
            benchmark/sha256_representation_benchmark.py

      - name: Run equivalence benchmark
        run: |
          python3 benchmark/sha256_representation_benchmark.py \
            z3 \
            --mode equiv \
            --equiv-rounds 2 4 8 16 \
            --trials 1 \
            --equiv-timeout 30

      - name: Check benchmark exports
        run: |
          test -s sha256sw_benchmark.json
          test -s sha256sw_benchmark.csv


  benchmark-collision:
    name: Reduced-round Collision Benchmark
    runs-on: ubuntu-latest
    needs: formal
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Z3
        run: |
          sudo apt-get update
          sudo apt-get install -y z3

          python3 -m pip install --upgrade pip
          python3 -m pip install z3-solver

      - name: Verify Z3
        run: |
          z3 --version
          python3 -c "import z3; print('Z3 Python:', z3.get_version_string())"

      - name: Run reduced-round collision benchmark
        run: |
          python3 benchmark/sha256_representation_benchmark.py \
            z3 \
            --mode collision \
            --rounds 16 20 \
            --trials 1 \
            --timeout 30

      - name: Check benchmark exports
        run: |
          test -s sha256sw_benchmark.json
          test -s sha256sw_benchmark.csv

      - name: Upload benchmark results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: sha256sw-benchmark-results
          path: |
            sha256sw_benchmark.json
            sha256sw_benchmark.csv
          if-no-files-found: warn
